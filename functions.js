module.exports = {
    returnTwo: (num) => {
        return `integer is ${num}`
    }, 
    greeting: (name) => {
        return `Hello, ${name}`
    }, 
    add: (numOne, numTwo) => {
        return numOne + numTwo
    }
}